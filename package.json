{
  "name": "socket-chat",
  "version": "0.0.1",
  "description": "my socket.io app",
  "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
       "start": "node index.js"
    },
  "dependencies": {
  "express": "^4.17.1",
    "socket.io": "^4.1.2"
  }
}
